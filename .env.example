NEXT_PUBLIC_API_BASE_URL=
PYTHON_BIN=
# Path to fixed no-fly GeoJSON. On Docker/Render, mapNew.geojson is often a Git LFS pointer unless LFS files are pulled into the image; the API then falls back to data/fixed-zones.deploy-sample.json.
UAV_FIXED_MAP_PATH=
UAV_MAX_DISTANCE_KM=30
